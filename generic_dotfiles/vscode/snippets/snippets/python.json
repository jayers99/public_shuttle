{
  "PEP 723 Inline Script Metadata": {
    "prefix": "pep723",
    "body": [
      "# /// script",
      "# requires-python = \">=${1:3.12}\"",
      "# dependencies = [\"${2:package}\"]",
      "# ///",
      "",
      "$0"
    ],
    "description": "PEP 723 inline script metadata block for uv run"
  }
}
