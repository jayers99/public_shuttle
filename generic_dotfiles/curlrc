# Follow redirects
location

# Show HTTP error codes as exit codes
fail

# Connection timeout (seconds)
connect-timeout = 10

# Max time for entire operation
max-time = 300

# Retry on transient errors
retry = 3
retry-delay = 2
retry-max-time = 60
